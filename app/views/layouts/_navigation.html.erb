<ul class="nav">
  <li>
    <%= link_to "Ruby Canada", root_path, :class => 'brand' %>
  </li>
</ul>

<div class="btn-group nav_item">
  <a class="btn btn-small dropdown-toggle" data-toggle="dropdown" href="#">
    Categories
    <span class="caret"></span>
  </a>
  <ul class="dropdown-menu">
    <% Category.all.each do |category| %>
    <li><%= link_to category.name, category_path(category) %></li>
    <% end %>
    <li class="divider"></li>
    <li>
      <%= link_to(new_category_path) do %>
        <i class="icon-plus"></i>
        New Category
      <% end %>
    </li>
  </ul>
</div>

<div class='nav_item'>
  <%= link_to 'New Topic', new_topic_path, class: 'btn btn-small btn-success' %>
</div>

<div class='nav_item'>
  <%= link_to 'Register', new_user_path, class: 'btn btn-small btn-success' %>
</div>
